Question 
write some stept how git is working and  commands 
To start with, here’s a list of common commands you will use when working with Git:

git init — initializes a repository
git checkout <branch> — checks out a branch from repository into the working directory
git add <file> — adds a change in a file to a change set
git commit — commits a change set from the working directory into the repository